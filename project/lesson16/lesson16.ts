const sum = (a: number, b: number): number => {
    return a + b;
}//can tra du lieu

const handleLogs = (message: string): void => {
    console.log(">>>>massage:", message)
} //check khong can tra du lieu